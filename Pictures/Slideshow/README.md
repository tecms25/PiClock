Drop your own slideshow images in this folder when `web_slideshow_playlist = 0`
in `Clock/Config.py`. PiClock picks a random order from whatever image files
(`.jpg`, `.jpeg`, `.png`, `.gif`, `.bmp`, `.webp`) it finds here at startup.
